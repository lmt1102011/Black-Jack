{
  "name": "black-jack-online",
  "version": "1.0.0",
  "private": true,
  "description": "Online multiplayer Blackjack platform with React, Socket.IO, Firebase, and Render deployment.",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "test": "npm run test --workspace shared",
    "lint": "npm run lint --workspace client",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
